# P12D: MASR Short cooldown port + shadow integration

_Generated: 2026-04-30 23:50_

P12D 是 MASR Short 上 active 前的最後一個技術門檻。完成項目：
1. 把 backtest 內建 cooldown 邏輯移植到 live `MaSrShortStrategy`
2. 等價驗證 cooldown 移植（live signals = backtest signals exact）
3. shadow integration（含 cooldown consistency check）
4. 365-day batch verification（260 signals / 0 real_mismatches）

---

## Q1: Cooldown 從 backtest 移植到 live 是否成功？

**✅ PASS**。`scripts/_verify_masr_short_cooldown.py` 結果（fast.top3 config × 3 syms × 12m）：

| Symbol | bt | live | exact | cooldown_rejected | real_mismatches |
|--------|---:|-----:|------:|------------------:|----------------:|
| BTCUSDT | 42 | 42 | 42 | 137 | **0 ✅** |
| ETHUSDT | 55 | 55 | 55 | 222 | **0 ✅** |
| SOLUSDT | 17 | 17 | 17 | 70 | **0 ✅** |

跟 P12B（cooldown 移植前）對比：

| Symbol | P12B bt | P12B live | Δ% | P12D bt | P12D live | Δ% |
|--------|--------:|----------:|---:|--------:|----------:|---:|
| BTCUSDT | 40 | 58 | +45% | 42 | 42 | **0** |
| ETHUSDT | 58 | 94 | +62% | 55 | 55 | **0** |
| SOLUSDT | 20 | 33 | +65% | 17 | 17 | **0** |

(BTC/ETH/SOL P12B vs P12D bt 數略不同是因 P12C.5 換成 fast.top3 config，sweep
找的 `LOOKBACK=150 / TOL=0.4 / SL=2.5` 過濾調整所致；同一 fast.top3 下
P12D verifier 跑得到 bt=42/55/17 完全跟 P12C.5 一致。)

關鍵實作：
- `strategies/ma_sr_short.py:on_position_close(symbol, exit_reason, exit_time)` — 只在 raw SL（"SL" in reason 且 "BE" not in reason）觸發 cooldown，duration = `MASR_SHORT_COOLDOWN_BARS or COOLDOWN_BARS` × timeframe
- `strategies/ma_sr_short.py:check_signal` 開頭加 cooldown gate（mirror backtest `if i <= cooldown_until: continue`）
- `bot_main.py` `_dispatch_position_close()` + 註冊到 `syncer/executor.set_post_close_callback()` —— 對 long 是 no-op（hasattr fallback）

---

## Q2: shadow_runner_short 是否 wired 完成 + 任意 acceptable diff？

**✅ wired 完成**。`shadow_runner.py` 加 `shadow_compare_signal_short(strategy_name, symbol, bar_time, live_signal, df_klines_*, in_cooldown, variant)`，跟 long 同結構但加 cooldown 一致性檢查：
- `in_cooldown=True` 但 `live_signal != None` → `cooldown_violation` real_mismatch（live 端 cooldown gate bug）

`SHORT_KNOWN_ACCEPTABLE_DIFFS = {}` — **空**。P12B 等價 6/6 + P12D 365d batch 都 0 mismatch；helper 跟 backtest 100% 等價，目前無 acceptable diff。預留結構以備 paper 期間發現新 boundary case。

`MaSrShortStrategy.check_signal` 在訊號通過 `validate_signal` 後呼叫 shadow，real_mismatch → `notifier.error()` + `log.error()`；exception 吞掉不阻塞 live。

---

## Q3: _verify_shadow_initial_short 365d batch 結果

**✅ PASS**。Universe = 7 非-cfd 幣（BTC/ETH/SOL/XRP/DOGE/PEPE/SKYAI）×
365 days × 1H K 線 × variant=fast：

```
=== SUMMARY ===
total_bars_processed:    58772
total_signals_generated: 260
total_cooldown_rejected: 1056
total_exact_matches:     260   ← = signals_generated
total_acceptable_diffs:  0
total_real_mismatches:   0     ← 必須 0 ✅
```

per-symbol:

| Symbol | bars | signals | cooldown_rej | exact | real_mismatches |
|--------|-----:|--------:|-------------:|------:|----------------:|
| BTCUSDT | 8424 | 42 | 137 | 42 | 0 |
| ETHUSDT | 8424 | 55 | 222 | 55 | 0 |
| SOLUSDT | 8424 | 17 | 70 | 17 | 0 |
| XRPUSDT | 8424 | 43 | 158 | 43 | 0 |
| DOGEUSDT | 8424 | 33 | 88 | 33 | 0 |
| 1000PEPEUSDT | 8424 | 37 | 202 | 37 | 0 |
| SKYAIUSDT | 8228 | 33 | 179 | 33 | 0 |

> **Note**：原 task 要求 30 天，但當前 crypto 牛市使 MASR_SHORT regime gate 在最近 30 天 0 訊號。延長至 365 天才實質 exercise 完整 path（cooldown gate + signal compare + on_position_close）。

---

## Q4: BACKLOG 三項處理

更新 `scripts/BACKLOG.md`：

1. **#4 cooldown drift**：✅ **CLOSED**（P12D 解決）
   - 標記 `~~MASR Short v2: cooldown drift between live and backtest~~ ✅ RESOLVED (P12D)`
   - 加證據連結（reports/p12d_*）

2. **#4-bis MASR Long 也應加對稱 cooldown gate**：✅ 新增為 backlog 條目
   - 等 short testnet paper 1-2 週證明穩定後對稱移植
   - Long 訊號量本來就少（4H），cooldown drift 影響相對小於 short（1H）

3. **#4-ter .env.example 跟 config.py 雙處 default 對齊機制**：✅ 新增為 backlog 條目
   - P12C.5 / P12D 各觸發一次 sync pain
   - 累積 3-5 個案例後再做自動化檢查腳本

---

## Q5: testnet 短倉 deploy 預備清單是否齊全？

**✅ 齊全**。`docs/testnet_deploy.md` 末尾加新段「Adding short pair (P12D 完成後)」涵蓋：

- **前置條件**：long testnet ≥ 1 週 + shadow_long=0 + initial_short PASS
- **Stage S1**：.env 加 short config（從 .env.example 抄）+ ACTIVE_STRATEGY 改為 long+short
- **Stage S2**：4 個 pre-deploy verification scripts 全 PASS
- **Stage S3**：tmux 啟動 + 24h 觀察 log expected 行
- **Stage S4**：1 週後檢查表（要看 / 不要看的指標）
- **Stage S5**：切 main net 條件（5 個門檻，out of P12D scope）

---

## 範圍遵守確認

✅ 不動 backtest logic（cooldown 從 backtest 抄到 live）  
✅ 不動 sweep 結果（P12C fast.top3 維持 baseline）  
✅ 不動 ACTIVE_STRATEGY（仍 ma_sr_breakout long-only）  
✅ 不上 main net live  
✅ 不動 .env（用戶手動套）  
✅ 不動 MASR Long 任何 logic（4-bis 留 backlog）  
✅ pytest 69 passed (no regression)

---

## 證據檔案

| 檔案 | 內容 |
|------|------|
| `reports/p12d_cooldown_logic.md` | cooldown 邏輯規格映射（任務 1.1） |
| `scripts/_verify_masr_short_cooldown.py` | 任務 2 等價驗證 driver |
| `scripts/_verify_shadow_initial_short.py` | 任務 4 365d batch driver |
| `scripts/strategies/ma_sr_short.py` | live 移植目標（cooldown gate + on_position_close + shadow hook） |
| `scripts/shadow_runner.py` | shadow_compare_signal_short + SHORT_KNOWN_ACCEPTABLE_DIFFS |
| `scripts/bot_main.py` | strategy registry + post-close dispatcher |
| `scripts/position_syncer.py` / `scripts/order_executor.py` | post-close callback hook |
| `.env.example` / `scripts/config.py` | MASR_SHORT_COOLDOWN_BARS env |
| `scripts/BACKLOG.md` | #4 closed, #4-bis / #4-ter 新增 |
| `docs/testnet_deploy.md` | "Adding short pair" 段落 |

---

## 下一步建議

1. **用戶 review 報告**：確認 cooldown 邏輯 + shadow 設計符合預期
2. **不上 active**：本輪結束時 ACTIVE_STRATEGY 維持 `ma_sr_breakout`
3. **Long testnet paper**（如還沒跑）：先按 `docs/testnet_deploy.md` 跑 long 1 週
4. **Short testnet deploy**：long 跑穩後按 `Adding short pair` 段落加 short
5. **P13 候選**：
   - 對稱移植 cooldown gate 到 MASR Long（BACKLOG #4-bis）
   - .env / config.py default 對齊自動化（BACKLOG #4-ter）
   - 跟蹤 shadow_short_mismatch 真實 alert 案例（如有）
