# P12C.5: MASR Short fast.top3 config apply + 等價驗證

_Generated: 2026-04-30 23:30_

P12C 已選定 fast.top3 為最終 config。本輪是 **P12D 前的橋接任務**：
- `.env.example` 套用新 config
- 確認 logic 邏輯沒被改 + 驗證 live ↔ backtest 在新 config 下仍等價
- 不寫新 code、不做 shadow integration

---

## 變更摘要

### `.env.example` diff

```diff
-MASR_SHORT_RES_LOOKBACK=100
+MASR_SHORT_RES_LOOKBACK=150
-MASR_SHORT_RES_TOL_ATR_MULT=0.3
+MASR_SHORT_RES_TOL_ATR_MULT=0.4
-MASR_SHORT_VOL_MULT=1.5
+MASR_SHORT_VOL_MULT=1.2
-MASR_SHORT_SL_ATR_MULT=1.2
+MASR_SHORT_SL_ATR_MULT=2.5
-MASR_SHORT_TP1_RR=2.0
+MASR_SHORT_TP1_RR=1.5
-MASR_SHORT_VARIANT=slow
+MASR_SHORT_VARIANT=fast
```

`MASR_SHORT_VOL_MULT` 從 1.5 → 1.2 是修正回 v2 backtest 用的數值（原本 1.5 是 v1 的，v2 backtest 內讀的是 `MASR_SHORT_V2_VOL_MULT=1.2`；live 路徑直接讀 `MASR_SHORT_VOL_MULT` 所以對齊到 1.2）。

### `scripts/config.py` diff

- `MASR_SHORT_VARIANT` default: `"slow"` → `"fast"`
- 註解更新指向 P12C 證據

### `scripts/strategies/ma_sr_short.py` diff

- 模組頂部 + class docstring 補上 P12C 證據（fast.top3 ROBUST adj +124.23U）
- 加上 "Production config: P12C fast.top3" + 報告路徑指引
- **logic code 完全沒動**

### `.env`（gitignored）

**未動**。用戶 deploy 時手動套 .env.example diff。

---

## 等價驗證（fast.top3 config）

```
=== Config readback (fast.top3) ===
  MASR_SHORT_VARIANT          = fast
  MASR_SHORT_RES_LOOKBACK     = 150
  MASR_SHORT_RES_TOL_ATR_MULT = 0.4
  MASR_SHORT_TP1_RR           = 1.5
  MASR_SHORT_SL_ATR_MULT      = 2.5
```

| Symbol | bars | bt signals | live signals | exact | cooldown_acc | **real_mismatches** |
|--------|-----:|-----------:|-------------:|------:|-------------:|--------------------:|
| BTCUSDT | 8484 | 42 | 54 | 42 | 12 | **0 ✅** |
| ETHUSDT | 8484 | 55 | 71 | 55 | 16 | **0 ✅** |
| SOLUSDT | 8484 | 17 | 24 | 17 | 7 | **0 ✅** |

```
✅ MASR Short v2 fast.top3 EQUIVALENCE VERIFIED
   3 syms × 12m × variant=fast under sweep-found config
   live signals all match backtest exact (entry/sl/tp/score)
```

**結論：PASS** — 所有 backtest signals 都被 live helper 完全等價 reproduce。多餘的 live signals 100% 落在 backtest cooldown 區間內（known acceptable，留 P12D 處理 cooldown drift）。

---

## 訊號量變化（vs P12B 舊 config）

P12B 用 baseline config（`LOOKBACK=100, TOL=0.3, TP1=2.0, SL=1.2`）；P12C.5 用 fast.top3（`LOOKBACK=150, TOL=0.4, TP1=1.5, SL=2.5`）。同 12m 期間 fast variant：

| Symbol | P12B fast bt | P12C.5 fast bt | Δ% | P12B fast live | P12C.5 fast live | Δ% |
|--------|-------------:|---------------:|---:|---------------:|-----------------:|---:|
| BTCUSDT | 40 | 42 | +5% | 58 | 54 | -7% |
| ETHUSDT | 58 | 55 | -5% | 94 | 71 | -24% |
| SOLUSDT | 20 | 17 | -15% | 33 | 24 | -27% |
| **total** | **118** | **114** | **-3%** | **185** | **149** | **-19%** |

**訊號量符合 sweep 結果預期**：
- P12C sweep 39m × 10 幣 fast.top3 n=1012；單幣 12m 平均 ~34 trades → 跟 P12C.5 BTC 42 / ETH 55 / SOL 17 在合理範圍（注意 sweep 是 39m 跨 10 幣，這裡是 12m × 3 幣，比例合理）。
- backtest 訊號量小幅下降（−3%）：新 config 的 LOOKBACK=150 + TOL=0.4 過濾掉一些邊緣訊號。
- live 訊號量下降較多（−19%）：因為 SL=2.5 比 SL=1.2 寬，trade 持倉時間變長 → cooldown 後仍在內、live helper 在 cooldown 區間外的有效訊號更少。
- 訊號**量的減少**配合 sweep audit 顯示的 **WR 從 45% 跳到 61%、wr_std 3.0 → 2.2pp**，正是「少而精」的預期表現。

---

## Smoke test + pytest

```
$ python scripts/_smoke_test_masr_short.py
  [1/4] MaSrShortStrategy init slow + fast OK
  [2/4] MaSrShortV1Deprecated 標明 v1_deprecated, name=ma_sr_short_v1_deprecated
  [3/4] feature_filter cfd default = ['cfd']
  [4/4] screen_coins cfd filter (slow + fast variants) PASS
✅ MASR Short v2 smoke test ALL PASSED

$ python -m pytest scripts/test_*.py -q
69 passed in 0.66s   (P10/P11/P12 既有測試全綠)
```

---

## 推薦下一步

**P12D — Shadow integration with cooldown 驗證**

需要做的事：
1. live `check_signal` 訊號產生時呼叫 `_v2_check_at_bar` 對應的 backtest path 比對訊號（同 MASR Long 的 shadow_runner pattern）
2. **特別檢查 cooldown drift**（BACKLOG #4）：live 訊號發生 bar 是否在 backtest cooldown 區間內？
3. real_mismatch alert + 寫 `reports/shadow_diffs/` (acceptable diffs 包含 cooldown asymmetry)
4. 收集 1-2 週 testnet paper 數據後決定 cooldown 模型對齊方向

**P12D 完成後才能討論**：
- ACTIVE_STRATEGY 是否加 ma_sr_short
- testnet checklist 加 short pair 段落
- live 真實 PnL vs backtest 預期的差距是否符合 BACKLOG #4 的 30-60% drift

---

## 範圍遵守確認

✅ 純 config + 驗證，沒寫新 logic code  
✅ `.env` 未動（用戶手動套）  
✅ ACTIVE_STRATEGY 維持 `ma_sr_breakout`（long-only）  
✅ 沒做 shadow integration（留 P12D）  
✅ pytest 69 passed (no regression)  
✅ 等價驗證 0 real_mismatches → 可 commit  
